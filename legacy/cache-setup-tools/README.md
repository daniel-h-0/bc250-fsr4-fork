# Retained cache-setup qualification tools

Exact tools from commit `67870dfc97f581597fbe4debfa1e8db67d30baeb`.
The September 14 cache-setup records pin these sources. The real-game record
further identifies the tools used in each launch and compares the cache
preparation code with this baseline. These are evidence, not installation
instructions; use the maintained files under `scripts/`.

The current helper retains the same measured preparation/launch code. Later
installer and diagnostic changes have separate review and userspace records in
`docs/legacy/research/cache-review.md`. Do not update historical hashes to represent new tests.
